<h1>Category show page</h1>
<%= @category.name %>
<ul>
  <% @products.each do |product| %>
  <li>
    <%= product.title %>
    <%= product.description %>
    <%= product.created_at %>
  </li>
  <% end %>
</ul>

